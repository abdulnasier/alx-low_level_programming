#include <stdio.h>
#include <unistd.h>

/**
 * main - main function
 *
 * Return: Always 0
 *
 */

int main(void)
{
	int x;
	char z;

		if (x > 9 && x < 16)
		{
			for (x = 10 ; x < 16 ; x++)
			{
				char a = 'a'

				putchar(a);
				a++;
			}
		}
		if (x >= 0 && x <= 9)
		{
			for (x = 0 ; x <= 9 ; x++)
				putchar(x + '0');
		}

	putchar('\n');
	return (0);
}
